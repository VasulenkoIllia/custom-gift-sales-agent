# Secrets — NEVER commit
.env
.env.deploy
.env.*.local

# Local-only compose override (publishes DB port for host dev; never on server)
docker-compose.override.yml

# Dependencies / build
node_modules/
dist/

# DB snapshots
.backups/

# Logs / OS
*.log
npm-debug.log*
.DS_Store

# Editor
.idea/
.vscode/
